# Same-origin API for the Vercel single-domain deploy. An empty base makes the
# API client issue relative requests (/api/...) against whatever host serves the
# app, so cookies are first-party and there is no CORS. Baked in at build time.
VITE_API_BASE_URL=
